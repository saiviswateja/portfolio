# Sai Portfolio (React + Vite + Tailwind)

Minimal, fast, JSON‑driven portfolio. Deployable to GitHub Pages.

## 🚀 Quick Start
```bash
npm install
npm run dev
```

## 🧱 JSON‑Driven Content
- Edit `src/data/about.json`, `experiences.json`, `articles.json` to update the site without touching code.

## 🌐 GitHub Pages Deploy
1. Commit and push this project to a repository named **REPO_NAME**.
2. Edit `vite.config.js` → set `base` to `"/REPO_NAME/"`.
3. Run:
```bash
npm run deploy
```
4. In GitHub, enable **Settings → Pages → Deploy from branch: gh-pages** (if not auto‑enabled).

## 🔧 Local Preview of Production Build
```bash
npm run build
npm run preview
```

## 🧼 Linting/Extras
You can add ESLint/Prettier later. This template is intentionally minimal.

